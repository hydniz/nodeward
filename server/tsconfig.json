{
  "// note": "node runs the .ts sources directly (type stripping, node >= 22.6), so this config only type-checks: `npm run typecheck -w server`. That is why imports carry .ts extensions and why only erasable syntax is allowed (no enums, no parameter properties, no namespaces).",
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "NodeNext",
    "moduleResolution": "nodenext",
    "types": ["node"],

    "strict": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,

    "verbatimModuleSyntax": true,
    "erasableSyntaxOnly": true,
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,

    "allowJs": true,
    "checkJs": false,

    "noEmit": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*.ts", "src/**/*.js"]
}
