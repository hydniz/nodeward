// loaded once before every test file (see vite.config.js → test.setupFiles).
// jest-dom adds matchers like toBeInTheDocument / toHaveTextContent; cleanup
// after each test comes from vitest globals + testing-library automatically.
import '@testing-library/jest-dom/vitest';
